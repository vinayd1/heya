<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>heya!</title>
    <link rel="icon" type="image/gif" href="../images/logo.png" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<style>
    html {
        background: url(../images/blur.jpg) no-repeat;
        background-size: cover;
        height: 100%;
        overflow-y: hidden;
    }
    .container{
        position:relative;
        padding:0 5%;
        height: 95vh;
        width: 85%;
        margin: 1% auto;
        background-color: ghostwhite;
        box-shadow: 5px 5px 5px 5px rgba(0, 0, 0, .4);
        overflow-y: auto;
        overflow-x: hidden;
    }
    #result{
        max-height: 60%;
        overflow-y: auto;
    }

    #result::-webkit-scrollbar {
        width: 10px;
    }
    #result::-webkit-scrollbar-thumb {
        background-color: #A569BD;
        border-radius: 20px;
    }

    .input-field input[type=text]:focus + label {
        color: #A569BD !important;
    }
    .input-field input[type=text]:focus {
        border-bottom: 1px solid #A569BD !important;
        box-shadow: 0 1px 0 0 #A569BD !important;
    }
    .btn-large{
        background-color: #A569BD;
    }
    .btn-large:hover{
        background-color: #BB8FCE;
    }
    .btn-large:focus {
        background-color: #A569BD;
    }
    .btn-large:focus:hover {
        background-color: #BB8FCE;
    }
    #dis {
        background-color: #FB1616;
    }
    #dis:hover{
        background-color: #FE4141;
    }
    #dis:focus {
        background-color: #FB1616;
    }
    #dis:focus:hover {
        background-color: #FE4141;
    }
    .point:hover {
        cursor:pointer;
        opacity:0.7;
    }

    .bubble{
        border-radius: 5px;
        box-shadow: 0 0 6px #B2B2B2;
        display: inline-block;
        padding: 10px 18px;
        position: relative;
        vertical-align: top;
    }
    .bubble::before {
        content: "\00a0";
        display: block;
        height: 16px;
        position: absolute;
        top: 11px;
        transform:             rotate( 29deg ) skew( -35deg );
        -moz-transform:    rotate( 29deg ) skew( -35deg );
        -ms-transform:     rotate( 29deg ) skew( -35deg );
        -o-transform:      rotate( 29deg ) skew( -35deg );
        -webkit-transform: rotate( 29deg ) skew( -35deg );
        width:  20px;
    }

    .you {
        background-color: #e6e6ed;
        float: left;
        margin: 5px 45px 5px 20px;
    }
    .you:before {
        background-color: #e6e6ed;
        box-shadow: -2px 2px 2px 0 rgba( 178, 178, 178, .4 );
        left: -9px;
    }

    .me {
        background-color: #edc98f;
        float: right;
        margin: 5px 20px 5px 45px;
    }
    .me::before {
        background-color: #edc98f;
        box-shadow: 2px -2px 2px 0 rgba( 178, 178, 178, .4 );
        right: -9px;
    }
    .addEmoticon:hover{
        cursor:pointer;
    }
    .addEmoticon{
        font-size:30px;
        display: inline-block;
    }
    #emoticonTray{
        max-width:22%;
        max-height: 30%;
        background-color: rgba(231, 51, 255, 0.3);
        border-radius: 3%;
        opacity: 0;
        padding:10px;
        overflow-x: hidden;
        overflow-y: auto;
        z-index: 100;
        position: absolute;
        display: inline-block;
        top: 53%;
        left: 66%;
    }
    #emoticonTray::-webkit-scrollbar {
        width: 7px;
    }
    #emoticonTray::-webkit-scrollbar-thumb {
        background-color: #A569BD;
        border-radius: 20px;
        outline: 1px solid slategrey;
    }

    #tri {
        width: 0;
        height: 0;
        border-left: 20px solid transparent;
        border-right: 20px solid transparent;
        border-bottom: 20px solid transparent;
        border-top: 20px solid rgba(231, 51, 255, 0.3);

        position: absolute;
        top: 83%;
        left: 80%;
        opacity: 0;
    }
</style>

</head>

<body>
<div class="container">
    <div class="row">
        <h2 class="center-align" style="font-family: 'cursive'; color: #9B59B6; font-weight: bolder !important; text-shadow:1px 1px purple;margin: 1rem 0 1rem 0">heya!</h2>
        <div style="font-weight: bold; margin: 0" class="center-align"><h4 style="margin: 0">{{firstname}}</h4></div>
        <div><h6 class="center-align" style="margin: 0">{{username}}</h6></div>
    </div>
    <hr>
    <div id="result"></div>
    <div id="emoticonTray" ></div><div id="tri"></div>
    <div id="userChat" style="position:absolute;bottom:0;width: 95%">
        <div class="row">
            <div class="col s1">
                <button class="waves-effect waves-light btn-large" id="dis"><i class="material-icons" style="font-size:30px;">call_end</i></button>
            </div>
            <div class="input-field col s8">
                <input type="text" id="inp" class="validate">
                <label for="inp" id="lab">Enter Text</label>
            </div>
            <div class="col s1"><button class="waves-effect waves-light btn-large" id="openEmoticons" style="width: 100%"><i class="material-icons">insert_emoticons</i></button></div>
            <div class="col s1">
                <button class="waves-effect waves-light btn-large" id="b"><i class="material-icons" style="font-size:30px;">send</i></button>
            </div>
        </div>
    </div>

</div>
<script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
<script src="/socket.io/socket.io.js"></script>
<script>
    let socket = io();
    let inp = document.getElementById('inp');
    let btn = document.getElementById('b');
    let result = document.getElementById('result');
    let dis = document.getElementById('dis');
    let count = 0;
    let emojis = [];
    socket.on('recieve', function(data){
        result.innerHTML += `<div class=" point bubble you"><span style="color: blue; font-weight: bold">${data.firstname}:</span> ${data.msg}</div><br><br><br>`;
        result.scrollTop = result.scrollHeight;
    });

    inp.onkeydown = function (ev) {
        if(ev.keyCode === 13)
            btn.onclick();
    };
    btn.onclick = function() {
        if(inp.value === '')
            return;
        socket.emit('send', {firstname: '{{firstname}}', msg: inp.value});
        result.innerHTML += `<div class="point bubble me"><span style="color: red; font-weight: bold"> You:</span> ${inp.value}</div><br><br><br>`;
        inp.value = "";
        result.scrollTop = result.scrollHeight;
    };
    socket.emit('con', '{{firstname}}');
    socket.on('new_connect', function(data) {
        result.innerHTML += `<div class="center-align">${data.data} Connected, Total Connections: ${data.count}</div>`;
        result.scrollTop = result.scrollHeight;
    });
    socket.on('disconnected-user', function(data){
        result.innerHTML += `<div class="center-align">1 User Disconnected, Total Connections: ${data.count}</div>`;
        result.scrollTop = result.scrollHeight;
    });
    socket.emit('total');
    socket.on('total', function (data) {
        result.innerHTML += `<div class="center-align">Total Connections: ${data.count}</div>`;
    })
    dis.onclick = function(){
        let confirm = window.confirm("Do you want to Disconnect?");
        if(confirm === true) {
            socket.disconnect();
            window.location.href = "/";
        }
    };
    $.get({
        'url': `/user/emoji?query=`,
        'success': function (res) {
            emojis = res;
        }
    });
    document.getElementById('openEmoticons').onclick = function () {
        count++;
        document.getElementById('emoticonTray').innerHTML = '';
        if (count % 2 === 0){
            document.getElementById('emoticonTray').style.opacity = 0;
            document.getElementById('tri').style.opacity = 0;
            return;
        }
        document.getElementById('emoticonTray').style.opacity = 1;
        document.getElementById('tri').style.opacity = 1;
        for (let i = 0; i < emojis.length; i++) {
            document.getElementById('emoticonTray').insertAdjacentHTML('beforeend', `<span class="addEmoticon" onclick="addEmoticon(this)">${emojis[i].emoji}</span><span>&nbsp</span>`);
        }
    };
    function addEmoticon(element){
        document.getElementById('lab').classList.add('active');
        inp.value += element.innerHTML;
    }
</script>

</body>
</html>
